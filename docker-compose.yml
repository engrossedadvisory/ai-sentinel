version: '3.8'

services:
  backend:
    build: ./backend
    container_name: governance-backend
    ports:
      - "8000:8000"
    volumes:
      - governance_data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/governance.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    build: ./frontend
    container_name: governance-frontend
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"

  # ── Optional: local Ollama for on-premise AI ─────────────────────────────
  # Uncomment to run a local LLM alongside the platform.
  # Then set AI_PROVIDER=ollama (or per-brain vars) in .env
  #
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: governance-ollama
  #   volumes:
  #     - ollama_models:/root/.ollama
  #   ports:
  #     - "11434:11434"
  #   restart: unless-stopped
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: all
  #             capabilities: [gpu]

volumes:
  governance_data:
    driver: local
  # ollama_models:
  #   driver: local
